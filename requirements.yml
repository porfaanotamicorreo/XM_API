name: entorno_api
dependencies:
  - python>=3.10
  - pip
  - wheel
  - setuptools
  - pip:
      - pandas>=2.2.3,<=3.0.3
      - numpy
      - pydataxm
      - requests
      - aiohttp
      - asyncio
      - nest_asyncio